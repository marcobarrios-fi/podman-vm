# Podman Virtual Machine Unprivileged Ports Configuration
# /etc/sysctl.d/unprivileged-ports.conf

# Allow non-root users to bind ports 80 and above
# (non-root users cannot bind to ports below 1024 by default)

net.ipv4.ip_unprivileged_port_start = 80